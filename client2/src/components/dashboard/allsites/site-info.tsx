'use client';
import * as React from 'react';
import { Paper } from '@mui/material';

export default function SiteInfo(): React.JSX.Element {
  return (
    <Paper sx={{p:2, width:{ xs:1, md: '40%' }}} elevation={10}>
        SiteInfo
    </Paper>
  );
}